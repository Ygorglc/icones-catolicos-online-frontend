import { DatePipe } from '@angular/common'; import { Component, DestroyRef, inject, OnInit, signal } from '@angular/core'; import { takeUntilDestroyed } from '@angular/core/rxjs-interop'; import { RouterLink } from '@angular/router'; import { CertificadoAdmin } from '../../models/admin.model'; import { AdminService } from '../../services/admin.service';
@Component({selector:'app-certificados-admin-page',imports:[DatePipe,RouterLink],templateUrl:'./certificados-admin-page.html',styleUrl:'../admin-table.scss'}) export class CertificadosAdminPage implements OnInit{private readonly service=inject(AdminService);private readonly destroyRef=inject(DestroyRef);protected readonly items=signal<CertificadoAdmin[]>([]);protected readonly message=signal<string|null>(null);ngOnInit(){this.load();}protected load(){this.service.certificados().pipe(takeUntilDestroyed(this.destroyRef)).subscribe({next:x=>this.items.set(x),error:()=>this.message.set('Não foi possível carregar os certificados.')});}}
