import { provideHttpClient } from '@angular/common/http'; import { HttpTestingController, provideHttpClientTesting } from '@angular/common/http/testing'; import { TestBed } from '@angular/core/testing'; import { API_BASE_URL } from '../../../core/config/api.config'; import { SenhaService } from './senha.service';
describe('SenhaService',()=>{let service:SenhaService;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting(),{provide:API_BASE_URL,useValue:'http://api.test/api'}]});service=TestBed.inject(SenhaService);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('should request password recovery',()=>{service.solicitarRecuperacao('cliente@teste.com').subscribe();const req=http.expectOne('http://api.test/api/auth/senha/recuperacao');expect(req.request.body).toEqual({email:'cliente@teste.com'});req.flush({mensagem:'ok'});});it('should redefine password using the temporary token',()=>{service.redefinir('token','NovaSenha123!').subscribe();const req=http.expectOne('http://api.test/api/auth/senha/redefinicao');expect(req.request.body).toEqual({token:'token',novaSenha:'NovaSenha123!'});req.flush(null);});it('should change the authenticated password',()=>{service.alterar('Atual123!','NovaSenha123!').subscribe();const req=http.expectOne('http://api.test/api/clientes/me/senha');expect(req.request.method).toBe('PUT');expect(req.request.body).toEqual({senhaAtual:'Atual123!',novaSenha:'NovaSenha123!'});req.flush(null);});});
